J'ai coder le projet avec la version de ruby en 2.5.5, il est probable que vous effectuez un bundle install avant lancer mon projet, j'ai déjà modifier la version sur mon gemfile. Quand a la version de rails, elle tourne sur la bonne version c'est a dire 5.2.3 merci.

